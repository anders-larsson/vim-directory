Installation:

    git clone git://git.thethundertemplars.com/vim.git .vim

- Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

- Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update

Add a new plugin:

    cd ~/.vim
    git submodule add http://github.com/vimrepo.git bundle/vimrepo
    git add .
    git commit -m "Install vimrepo.vim bundle as a submodule."


Spelling files:
    ftp://ftp.vim.org/pub/vim/runtime/spell/
    Current date of files: 08/09/2010

Source: http://vimcasts.org/episodes/synchronizing-plugins-with-git-submodules-and-pathogen/
